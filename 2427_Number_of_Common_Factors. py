class Solution(object):
    def commonFactors(self, a, b):
        count=0
        if a==b==1:
            return 1
        else:
            for i in range(1,a*b):
                if a%i==0 and b%i==0:
                    count+=1
            return count
